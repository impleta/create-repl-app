{
  "name": "create-repl-app",
  "version": "1.0.0",
  "description": "Creates a new REPL application using Replicant",
  "bin": {
    "create-repl-app": "./build/src/create-repl-app.js"
  },
  "files": [
    "build/src"
  ],
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm.cmd run compile",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run lint"
  },
  "keywords": [
    "REPL"
  ],
  "author": "Impleta",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.1",
    "typescript": "~4.7.0"
  },
  "dependencies": {
    "replicant": "file:../Replicant"
  }
}
